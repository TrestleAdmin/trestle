<% flatpickr_locale_conversions = { ca: "cat", el: "gr", nb: "no", vi: "vn" } %>

<% I18n.available_locales.each do |locale| %>
  <% locale = flatpickr_locale_conversions[locale] || locale %>
  <% require_asset("trestle/flatpickr/#{locale}") rescue nil %>
<% end %>
