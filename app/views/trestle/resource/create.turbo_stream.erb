<turbo-stream action="replace" target="<%= dom_id(admin.build_instance) %>">
  <template>
    <%= render template: instance.persisted? ? "trestle/resource/show" : "trestle/resource/new", formats: [:html] %>
  </template>
</turbo-stream>
